/**
 * STUB — replaced by the visual editor module.
 *
 * Contract (do not change without updating the admin app that mounts this):
 * - default export: React component
 * - props: { transformId: string }
 * - The component is responsible for loading the transform via
 *   GET /api/admin/transforms/:id and saving via PUT /api/admin/transforms/:id.
 */
export default function EditorScreen({ transformId }: { transformId: string }) {
  return <div style={{ padding: 24 }}>Visual editor loading… (transform: {transformId})</div>;
}
